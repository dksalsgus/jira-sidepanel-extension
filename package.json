{
  "name": "jira-sidepanel-extension",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:prod": "NODE_ENV=production node build.js",
    "watch": "node build.js --watch",
    "test": "node --test",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
