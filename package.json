{
  "name": "jira-sidepanel-extension",
  "version": "1.1.0",
  "description": "Chrome extension for checking assigned Jira issues from the side panel.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:prod": "NODE_ENV=production node build.js",
    "watch": "node build.js --watch",
    "test": "node --test",
    "clean": "rm -rf dist",
    "package": "bash scripts/package.sh"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
